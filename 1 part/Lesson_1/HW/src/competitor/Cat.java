package competitor;

import competitor.CompetitorParentClass;

public class Cat extends CompetitorParentClass {
    public Cat(String type, String name, int maxRunDistance, int maxJumpDistance, int maxSwimDistance, boolean onDistance) {
        super(type, name, maxRunDistance, maxJumpDistance, maxSwimDistance, onDistance);
    }
}
